{
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npm run css && webpack --mode production",
    "watch": "npm install && npm run css && webpack --watch --progress --colors",
    "css": "mkdir -p src/static/build && cp node_modules/@kbase/base-css/css/tachyons.min.css src/static/build/tachyons.min.css"
  },
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.3.4",
    "@babel/preset-env": "^7.3.4",
    "@babel/preset-react": "^7.0.0",
    "babel-eslint": "^10.0.1",
    "babel-loader": "^8.0.5",
    "eslint": "^5.15.3",
    "eslint-config-google": "^0.12.0",
    "preact-compat": "^3.18.4",
    "webpack": "^4.29.6",
    "webpack-bundle-analyzer": "^3.3.2",
    "webpack-cli": "^3.2.3"
  },
  "dependencies": {
    "@kbase/base-css": "0.0.2",
    "mitt": "^1.1.3",
    "preact": "^8.4.2",
    "timeago.js": "^4.0.0-beta.2"
  }
}
